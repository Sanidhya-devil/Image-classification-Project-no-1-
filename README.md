# 🔢 Digit Recognition using SVM (Scikit-Learn)

This project demonstrates how to build a **Handwritten Digit Recognition** system using the **Support Vector Machine (SVM)** classifier on the popular **Digits Dataset** from Scikit-Learn.

The dataset contains **8×8 pixel images** of handwritten digits (0–9).  
The goal is to train an ML model that can accurately predict which digit (0–9) is present in an image.

---

## 📌 Features

- Load & explore the digits dataset  
- Visualize sample images  
- Preprocess data (train-test split + scaling)  
- Train an SVM classifier  
- Predict test digits  
- Evaluate performance using  
  ✔ Classification Report  
  ✔ Confusion Matrix (Visualization)

---

## 🧠 Technologies Used

- Python  
- Scikit-Learn  
- Matplotlib  
- NumPy  
- Machine Learning (SVM)

---

